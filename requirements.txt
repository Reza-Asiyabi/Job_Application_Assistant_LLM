openai>=1.12.0
python-dotenv>=1.0.0
PyPDF2>=3.0.0

# Optional — only needed for import_tracker.py (XLSX import)
# pip install openpyxl
# openpyxl>=3.1.0

# Optional — only needed for URL import (Import from URL button)
# pip install requests beautifulsoup4
# requests>=2.31.0
# beautifulsoup4>=4.12.0
